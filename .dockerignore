.git
.venv
.pytest_cache
__pycache__
*.py[cod]
frontend/node_modules
frontend/.next
node_modules
.next
# Runtime state never enters an image: the signing key, proofs, reports,
# the database and every upload stay on the host that made them. The
# Dockerfile creates the empty directories instead.
storage
tests
docs
