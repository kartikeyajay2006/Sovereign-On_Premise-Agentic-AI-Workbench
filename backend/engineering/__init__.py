"""Deterministic engineering: unit-aware quantities, versioned formulas, and
assessments whose every input is bound to the evidence it was read from.

The language model explains and drafts. It is never the authority for
arithmetic: a figure that ends up in an approval note is computed here, by a
registered formula at a recorded version, from inputs that each name the
evidence item, row and column they came from.
"""
